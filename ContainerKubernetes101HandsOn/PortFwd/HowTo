$ docker run -d--name=myapache httpd
$ curl http://localhost
$ docker ps
$ docker container stop myapache
$ docker container rm myapache
$ docker run -d -p 8080:80 --name=myapache httpd
$ curl http://localhost:8080

*** important to mention the port number

$ docker inspect myapache
$ docker logs myapache
